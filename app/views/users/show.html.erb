  <h1><%= @user.username %></h1>
   <h2> Is a wonderful Crabby Movie Reviewer!! </h2>
   <p>  Need to reach them? </p>
        Here is <%= @user.username %>'s email address: <%= @user.email %>
   </p>

      <% if current_user.super_user? && !@user.super_user? %>
        <%= link_to 'Make Super User', user_path(@user),
              method: :patch,
              data: { confirm: 'Are you sure?' } %>
      <% end %>
